// src/models/House.js
export class House {
    constructor(boardPosition) {
        this.boardPosition = boardPosition;
    }

    visit(player, bank) {
        // Lógica de visita
    }

    buy(player, bank) {
        // Lógica de compra
    }
}
