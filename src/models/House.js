// src/models/House.js
export class House {
    constructor(boardPosition) {
        this.boardPosition = boardPosition;
        this.type = "";
    }

    visit(player, bank, io) {
        // Lógica de visita
    }
}
